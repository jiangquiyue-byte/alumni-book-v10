# 同学录系统 v10.0 开发文档

> **版本**：10.0  
> **作者**：Manus AI  
> **适用范围**：当前线上代码结构、后台管理系统、静态页面生成器、粒子系统、上传系统与日常维护

---

## 一、文档定位

本文档面向当前版本的开发者、维护者与后续二次开发人员，目标是准确描述 **同学录系统 10.0** 的真实代码结构与运行方式。该系统延续了轻量化设计：前台为静态页面集合，后台负责写入 JSON 数据、上传资源并生成 HTML 页面，因此不依赖数据库，也不要求复杂的服务端框架。

与旧版本相比，10.0 的关键变化主要集中在三个方向。第一，**粒子系统完成统一注册化改造**，前后台对同一组内置粒子使用一致命名，新增了枫叶、银杏、羽毛、心形、光泡、书页等效果。第二，**普通模板页与专属模板页的运行时逻辑更加统一**，全局字体同步、粒子初始化、资源缓存参数与公共脚本职责已经尽量收拢。第三，**文档与版本标识同步升级到 10.0**，便于后续维护不再依赖过时说明。

---

## 二、系统整体架构

同学录系统的核心并不是传统意义上的动态网站，而是一个“**后台管理 + JSON 数据层 + 静态页面生成**”的内容系统。管理员在后台录入资料、上传文件、调整站点配置后，PHP 接口会把数据写入 JSON 文件，并按模板生成或更新前台页面。前台用户访问时，绝大多数页面只依赖静态 HTML、CSS 和 JavaScript。

| 层级 | 组成 | 主要职责 |
|---|---|---|
| 前台展示层 | `index.html`、`preface.html`、`roster.html`、`album.html`、`students/*.html` | 展示封面、前言、名册、相册和学生个人页 |
| 公共运行时层 | `assets/js/common.js`、`assets/js/overlay.js`、公共样式 | 初始化页面、应用字体、驱动粒子、维护交互效果 |
| 后台管理层 | `admin/index.php`、`admin/admin.php`、`admin/assets/js/*.js` | 登录、资料管理、上传、站点设置、生成与维护 |
| 接口层 | `admin/api/*.php` | 读写 JSON、处理上传、生成页面、返回统一 JSON 响应 |
| 数据层 | `data/*.json`、`album/*.json` | 存储学生信息、站点配置、名册、相册与自定义粒子数据 |
| 资源层 | `students/`、`album/`、`assets/images/overlays/` | 存储头像、背景、贴纸、音乐、照片、全景图与粒子素材 |

系统的主数据流可以概括为：管理员在后台操作，前端后台脚本调用 PHP 接口，接口更新 JSON 数据，必要时重新生成静态页面，再由前台页面和公共脚本读取与渲染。

---

## 三、目录结构说明

当前项目目录已经不再适合用旧版本的粗略树形图描述，而需要区分“前台站点本体”“后台应用”“数据与资源目录”三个部分。

```text
site/
├── index.html
├── preface.html
├── roster.html
├── album.html
├── README.md
├── ADMIN_GUIDE.md
├── 服务器配置说明.md
├── assets/
│   ├── css/
│   ├── js/
│   │   ├── common.js
│   │   └── overlay.js
│   └── images/
│       └── overlays/
├── admin/
│   ├── index.php
│   ├── admin.php
│   ├── setup.php
│   ├── templates/
│   │   ├── student.tpl.html
│   │   └── student_owner.tpl.html
│   ├── assets/
│   │   ├── css/
│   │   └── js/
│   └── api/
│       ├── _init.php
│       ├── auth.php
│       ├── generate.php
│       ├── site_config.php
│       ├── students.php
│       ├── upload.php
│       ├── stickers.php
│       ├── album.php
│       ├── panorama.php
│       └── custom_particles.php
├── data/
│   ├── students.json
│   ├── classmates.json
│   ├── site_config.json
│   ├── site_settings.json
│   └── custom_particles.json
├── students/
│   ├── *.html
│   ├── avatars/
│   ├── backgrounds/
│   ├── stickers/
│   ├── music/
│   └── photos/
└── album/
    ├── photos/
    ├── panoramas/
    └── photos.json
```

| 目录 / 文件 | 说明 | 是否经常改动 |
|---|---|---|
| `assets/js/common.js` | 前台公共运行时，负责初始化、字体应用、粒子引擎与公共交互 | 高 |
| `assets/js/overlay.js` | 10.0 粒子预设注册表，统一内置粒子定义 | 高 |
| `admin/assets/js/app.js` | 后台入口与公共状态协调 | 中 |
| `admin/assets/js/music-editor.js` | 音乐模块与粒子编辑器模块共存的关键脚本文件 | 高 |
| `admin/api/generate.php` | 模板渲染与学生页面生成器 | 高 |
| `admin/templates/*.tpl.html` | 普通模板与专属模板的 HTML 输出骨架 | 高 |
| `data/site_config.json` | 站点级前言、致谢、字体、页面粒子配置 | 高 |
| `data/students.json` | 学生资料主数据源 | 高 |

---

## 四、运行机制与数据流

该系统的运行机制可以理解为“**一次编辑，多处输出**”。后台并不直接渲染前台页面，而是把用户输入持久化到 JSON，再由生成器把这些数据编译成静态 HTML。因此，定位问题时一定要区分：问题是在后台表单层、接口保存层、模板生成层，还是在前台运行时层。

| 场景 | 触发入口 | 数据写入 | 后续动作 |
|---|---|---|---|
| 新建或修改学生资料 | 后台学生管理 | `data/students.json` | 重新生成对应学生页面 |
| 修改站点设置 | 后台站点设置 | `data/site_config.json` / `data/site_settings.json` | 前台页面下次加载即生效，部分页面需重新生成 |
| 上传头像 / 背景 / 音乐 / 贴纸 | 后台上传接口 | 资源目录 + 学生数据字段 | 更新页面引用路径 |
| 选择粒子效果 | 后台粒子编辑器 | 学生数据或站点配置 | 前台公共脚本读取预设后渲染 |
| 上传自定义粒子 | 后台自定义粒子模块 | `data/custom_particles.json` + `assets/images/overlays/` | 粒子选择器动态扩展 |

前台普通页面例如 `index.html`、`preface.html`、`roster.html`、`album.html`，主要通过公共脚本读取 `site_config.json`、名册数据或相册数据进行展示。而学生页则通常是后台模板生成好的静态 HTML，再通过公共脚本补足粒子、字体和少量运行时行为。

---

## 五、核心数据文件

### 5.1 学生主数据

学生资料的单一事实来源仍然是 `data/students.json`。后台各功能模块都围绕这份数据进行增删改查，生成器则根据每位学生的数据输出对应 HTML 页面。不要在 `students/*.html` 中手工维护通用字段，因为下次重新生成时会被覆盖；只有明确标记为专属定制的页面，才适合在模板或页面层做特殊处理。

### 5.2 站点配置数据

10.0 中最值得注意的站点级数据文件是 `data/site_config.json`。它不仅承担粒子开关，还管理前言、特别致谢和全局字体。当前实际结构包含 `particles`、`footer`、`preface`、`acknowledgments`、`typography` 等顶级字段。

| 字段 | 用途 | 当前说明 |
|---|---|---|
| `particles` | 控制首页、前言页、名册页、相册页、学生页的粒子开关和预设 | 页面级配置 |
| `footer` | 备案号、版权等底部信息 | 站点展示配置 |
| `preface` | 前言标题、副标题、正文 | 前言页内容源 |
| `acknowledgments` | 特别致谢列表 | 10.0 已支持头像 `avatar` 字段 |
| `typography` | 字体族与字号 | 前台公共脚本读取并应用 |

### 5.3 自定义粒子数据

10.0 保留了自定义粒子能力。内置粒子定义放在 `assets/js/overlay.js`，自定义粒子列表则继续写入 `data/custom_particles.json`。公共粒子注册表会在运行时加载该文件，把用户上传的粒子图片扩展到 `OverlayPresets` 中，因此新增自定义粒子不需要手改前台核心脚本。

---

## 六、前台公共运行时

前台公共运行时的核心是 `assets/js/common.js`。这一文件承担了大量横切能力，包括页面初始化、通用工具函数、粒子引擎、全局字体同步、部分布局修正，以及多个页面共享的交互逻辑。10.0 的重构方向，是把此前散落在模板或专属页面中的共性逻辑尽量收拢到这里，减少模板重复代码。

| 模块能力 | 所在文件 | 10.0 变化 |
|---|---|---|
| 工具函数与初始化 | `common.js` | 保持核心入口，便于所有页面共享 |
| Overlay 粒子引擎 | `common.js` | 增强参数支持，如漂移范围、时长、尺寸、透明度峰值 |
| 粒子预设注册 | `overlay.js` | 改为统一注册表 `BuiltinParticleRegistry` |
| 全局字体同步 | `common.js` | 普通模板页与专属页行为统一 |
| 模板运行时一致性 | 模板 + 公共脚本 | 统一版本参数与资源引用策略 |

### 6.1 10.0 粒子注册表

10.0 最重要的前台改动之一，是把内置粒子预设统一抽离到 `overlay.js`。当前注册表通过 `BuiltinParticleRegistry` 管理，并对外导出 `OverlayPresets`。后台、模板和页面运行时应尽量围绕同一套预设值工作，避免再出现“后台有选项、前台无定义”的分裂情况。

当前内置粒子包括以下类型：

| 预设值 | 显示名称 | 说明 |
|---|---|---|
| `sakura` | 樱花 | 经典花瓣效果 |
| `stars` | 星星 | 星点下落效果 |
| `snow` | 雪花 | 轻量雪花效果 |
| `confetti` | 彩纸 | 节庆彩纸飘落 |
| `fireflies` | 萤火虫 | 发光漂浮效果 |
| `bamboo` | 竹叶 | 东方风格叶片效果 |
| `maple` | 枫叶 | 新增，适合秋季氛围 |
| `ginkgo` | 银杏 | 新增，适合古朴与校园主题 |
| `feather` | 羽毛 | 新增，漂浮更柔和 |
| `hearts` | 心形 | 新增，适合告白与纪念主题 |
| `bubbles` | 光泡 | 新增，适合梦幻类页面 |
| `paper` | 书页 | 新增，贴合古朴书卷视觉 |
| `lite` | 轻量 | 低密度、低干扰模式 |
| `none` | 关闭 | 不启用粒子 |

### 6.2 Overlay 粒子引擎

`common.js` 中的 `Overlay` 对象已经不是早期的简单飘落器，而是一个支持多种配置项的统一引擎。10.0 扩展了 `driftRange`、`duration`、`size`、`opacityPeak`、`spawnDelay` 等参数，使不同粒子预设不仅仅通过图形区分，也可以通过运动节奏和透明度变化区分。后续若要继续增加内置粒子，优先做法是新增预设，而不是复制整套粒子引擎代码。

---

## 七、后台系统结构

后台入口由 `admin/index.php` 与 `admin/admin.php` 组成。前者负责登录页，当前已更新为古朴书籍风格；后者承载后台主界面。登录成功后，后台主要依赖 `admin/assets/js/` 目录下的模块化脚本运行，整体是偏轻量的原生 JavaScript SPA 结构。

| 后台组成 | 说明 |
|---|---|
| `admin/index.php` | 登录页，负责管理员鉴权入口 |
| `admin/admin.php` | 后台主页面容器 |
| `admin/assets/js/app.js` | 后台主入口，负责初始化模块与全局状态 |
| `admin/assets/js/api.js` | 封装接口调用 |
| `admin/assets/js/ui.js` | Toast、Modal、按钮 loading 等通用 UI 能力 |
| `admin/assets/js/*-editor.js` | 各业务模块，如学生、贴纸、粒子、音乐、内容管理等 |

10.0 并没有把后台完全重写成组件化框架，而是继续保持模块文件拆分、通过命名空间或 IIFE 挂载能力的方式。这一结构虽然轻量，但开发时要注意避免跨模块直接写死 DOM 依赖，最好复用统一 API 与 UI 层。

---

## 八、后台主要业务模块

### 8.1 学生管理与页面生成

学生资料管理仍然是后台的核心模块。所有学生数据更新最终都要落到 `students.json`，然后通过 `admin/api/generate.php` 把模板渲染为静态学生页。开发者如果新增字段，必须同时检查三处：后台编辑表单、保存接口、模板输出逻辑。

### 8.2 站点设置模块

站点设置负责前言、致谢、底部信息、字体与页面级粒子。字体现在通过 `typography.fontFamily` 和 `typography.fontSize` 统一保存，并由前台公共脚本应用。此前“后台改字号、旧模板页不变”的问题已经在近期修复，因此当前版本应优先通过公共脚本统一接管字体，而不是在模板里到处写死字号。

### 8.3 粒子编辑器

粒子编辑器的关键逻辑位于当前后台脚本中的粒子模块实现。10.0 已把内置粒子列表扩展到和前台注册表对齐，至少包括枫叶、银杏、羽毛、心形、光泡、书页等新效果。开发时需要牢记：后台列表只是“选择入口”，真正的视觉效果定义仍在前台公共运行时与 `overlay.js` 中。

### 8.4 贴纸编辑器

贴纸编辑器承担学生页面贴纸摆放与预览。此前已修复“移动端贴纸层级高于信息层”的问题，因此当前预览应遵循真实页面图层关系，贴纸位于信息填写层下方。后续如再改布局，必须同时检查前台模板与后台预览 iframe 的一致性。

### 8.5 上传模块

上传模块通过 `admin/api/upload.php` 处理不同类型文件，包括头像、背景、音乐、照片、贴纸以及粒子图片等。前端通常通过统一上传封装函数提交请求，并在 UI 层展示进度条。10.0 的调试经验表明，上传问题大多不是前端逻辑错，而是目录权限、上传上限、类型映射或接口鉴权问题，因此维护时应优先分层排查。

---

## 九、模板系统与页面生成器

系统当前包含两类主要学生模板：普通模板 `student.tpl.html` 与专属模板 `student_owner.tpl.html`。二者的目标不是完全一样：普通模板强调批量生成、结构稳定；专属模板允许更强的个性化表达。但在 10.0 中，这两类模板的公共依赖已经尽量统一，尤其是字体同步、脚本版本参数与粒子初始化入口。

| 模板 | 用途 | 10.0 关注点 |
|---|---|---|
| `student.tpl.html` | 普通学生页面批量生成 | 统一公共脚本引用、继承全局字体和粒子能力 |
| `student_owner.tpl.html` | 专属定制页面 | 减少与普通模板重复的运行时代码 |

`admin/api/generate.php` 负责把学生数据注入模板并输出 HTML。此文件在开发中非常关键，因为任何新增字段、路径、脚本版本参数或页面级默认值，最终都需要在这里拼装正确。若生成器遗漏更新，就会出现后台保存成功但页面不生效的情况。

---

## 十、版本管理与缓存策略

10.0 的另一项重要维护工作，是把版本号与资源缓存策略统一起来。项目当前已经在多个关键文件中更新为 **10.0** 标识，模板资源引用也已逐步追加版本参数，以避免浏览器缓存长期持有旧脚本。开发者在进行后续版本迭代时，应同步更新以下内容：对外版本展示、内部注释标识、模板中的资源版本参数，以及必要的文档版本号。

| 需要同步的位置 | 原因 |
|---|---|
| 后台部署页面 / 入口页中的版本展示 | 避免运维看到旧版本号 |
| API 初始化注释或内部标识 | 便于维护时识别版本基线 |
| 模板中 JS / CSS 的版本参数 | 避免脚本缓存导致“代码未生效”错觉 |
| README / 开发文档 / 服务器说明 | 保持文档与实际代码一致 |

---

## 十一、10.0 架构优化总结

虽然项目总体仍然采用原生 PHP 与原生 JavaScript，但 10.0 已经对“能统一的公共逻辑”做了比较明确的收敛。可以把这次重构理解为一次“**在保持轻量前提下的架构整理**”。

| 优化方向 | 旧问题 | 10.0 处理方式 |
|---|---|---|
| 粒子预设 | 前后台列表和前台定义容易分离 | 统一改为注册表驱动 |
| 粒子参数 | 预设差异主要靠图标与数量 | 扩展漂移、时长、尺寸、透明度等能力 |
| 模板运行时 | 普通页和专属页逻辑重复 | 更多公共逻辑收敛到 `common.js` |
| 字体同步 | 部分旧页面字号不跟随后台 | 通过公共脚本统一接管 |
| 缓存问题 | 改完脚本浏览器仍读旧文件 | 模板统一追加版本参数 |
| 文档一致性 | 旧文档版本落后严重 | 全部升级到 10.0 描述 |

需要强调的是，10.0 不是把项目改造成大型框架，而是在当前技术栈内提升一致性、可维护性和扩展效率。因此后续继续优化时，也应优先遵循这一原则：**优先抽象重复能力，不轻易引入复杂依赖**。

---

## 十二、二次开发建议

如果后续要继续扩展系统，建议遵循以下顺序：先确认数据结构是否需要新增字段，再补后台编辑表单与保存接口，然后更新生成器输出，最后补前台运行时能力。不要跳过中间层，尤其不要只改模板或只改前端脚本。

### 12.1 新增学生字段

新增字段时，应同步检查 `students.json` 数据结构、后台编辑界面、保存接口、模板占位与页面展示。如果该字段还需要在名册或首页摘要中显示，还要继续修改 `classmates.json` 同步逻辑或对应前台页面。

### 12.2 新增内置粒子

新增粒子时，推荐步骤如下：先在 `overlay.js` 中注册预设，再确认 `common.js` 的粒子引擎支持所需参数，然后同步更新后台粒子选择列表与生成器映射。若粒子属于图片型效果，也可以考虑走自定义粒子机制，而不是硬编码进核心脚本。

### 12.3 调整页面主题或视觉风格

如果是全站级视觉调整，优先修改公共样式与全局配置读取逻辑；如果只是某个专属页的独立设计，则尽量把个性化代码限制在专属模板内，避免反向污染普通模板。

---

## 十三、调试与排障建议

当前项目最有效的排障方法，不是盲目改代码，而是先判断问题处于哪个层级。多数问题都可以归类到以下四类之一：保存没成功、文件没上传成功、模板没重新生成、前台运行时没读到最新资源。

| 现象 | 优先检查项 |
|---|---|
| 后台保存成功但前台页面无变化 | 生成器是否执行、模板是否更新、资源是否被缓存 |
| 头像或致谢图片不显示 | 路径是否写入 JSON、文件是否真实存在、前台是否按字段读取 |
| 字体设置不生效 | `site_config.json` 是否更新、页面是否加载最新 `common.js` |
| 粒子选择后无效果 | 预设值是否存在、页面粒子是否启用、前台公共脚本是否为最新版本 |
| 上传失败 | 目录权限、PHP 上传限制、接口返回 JSON、前端类型参数 |

如果需要在生产环境快速定位问题，建议同时查看浏览器控制台、网络请求、服务器错误日志，以及对应 JSON 文件是否发生实际变化。这种“页面、接口、数据、文件系统”四层对照的方法，远比单看前端报错更有效。

---

## 十四、维护基线

为了保证 10.0 之后的持续可维护性，建议把下列内容视为版本维护基线：其一，`students.json` 与 `site_config.json` 是主要事实数据源，非必要不手工编辑生成后的 HTML；其二，`common.js` 与 `overlay.js` 是前台公共逻辑中心，应谨慎修改并同步模板缓存参数；其三，文档、版本号、后台展示与模板引用必须保持一致，否则维护成本会迅速上升。

| 维护对象 | 原则 |
|---|---|
| 数据文件 | 优先由后台接口维护，不直接手改 |
| 生成页面 | 除专属定制外，避免绕过生成器手工维护 |
| 公共脚本 | 每次修改后都做缓存与兼容性验证 |
| 文档 | 每次大版本迭代同步更新 |
| 上传目录 | 定期检查权限、空间与冗余文件 |

---

## 十五、结语

同学录系统 10.0 的价值不只在于新增了更多粒子效果，而在于它已经具备一套更清晰的维护路径：**后台负责结构化管理，模板负责静态输出，公共脚本负责统一运行时，文档负责约束后续开发方向**。只要后续开发继续沿着这条路径推进，这套系统可以在不引入重型框架的前提下，持续保持较好的可扩展性与可维护性。

*同学录系统 v10.0 开发文档*
