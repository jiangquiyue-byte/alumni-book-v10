# 同学录 v10.5 · 服务器配置说明

> 本文档面向服务器部署与运维，适用于当前 **PHP + 静态页面生成** 的同学录系统 10.5 版本。
>
> 当前版本已经统一前后台粒子预设注册、扩展了更多内置粒子效果，并继续采用 **后台写 JSON + 生成静态 HTML** 的运行方式。

---

## 一、适用环境

同学录 10.5 仍然采用轻量部署方式。前台页面是静态 HTML、CSS、JavaScript，后台通过 PHP 接口维护 JSON 数据并重新生成学生页面。因此，服务器不需要数据库，但必须保证 **PHP 可写目录权限**、**JSON 文件可读写**、**上传限制足够**。

| 项目 | 最低要求 | 建议值 |
|---|---|---|
| 操作系统 | Linux / Windows 均可 | Linux 服务器 |
| Web 服务 | Nginx 或 Apache | Nginx + PHP-FPM |
| PHP 版本 | 7.4+ | 8.1+ |
| 必需扩展 | `json`、`gd` | `json`、`gd`、`fileinfo` |
| 数据库 | 不需要 | 不需要 |
| HTTPS | 非必需 | 强烈建议启用 |

系统当前接口初始化逻辑中已主动提升上传与执行限制，默认会尝试设置上传文件上限与执行时间，以提高相册、音乐、全景图等资源管理的稳定性。因此，服务器侧配置不应低于应用层建议值，否则仍可能被 Web 服务或 PHP 全局配置拦截。

---

## 二、站点部署方式

项目推荐直接部署到网站根目录，保持前后台与静态资源的相对路径不变。若部署在子目录，也可以正常运行，但需确保 Web 访问路径与文件系统根路径保持一致，不要破坏 `admin/`、`assets/`、`data/`、`students/` 等目录层级。

| 目录 | 作用 | 是否需要写权限 |
|---|---|---|
| `data/` | 存储 `students.json`、`classmates.json`、`site_config.json` 等核心数据 | 是 |
| `students/` | 存储生成后的学生页面及头像、背景、贴纸、音乐、照片 | 是 |
| `album/` | 存储班级相册与全景图配置及资源 | 是 |
| `assets/images/overlays/` | 存储自定义粒子图片 | 是 |
| `admin/` | 后台程序与接口 | 否，但可读 |
| `assets/` | 前台通用静态资源 | 否，但可读 |

如果是首次部署，建议直接访问后台附带的一键部署脚本完成目录初始化；当前该脚本已经更新为 **v10.5** 标识，并会尝试创建必要目录、初始化 JSON 数据文件、设置权限、检测扩展与输出环境信息。

---

## 三、推荐目录权限

系统的核心风险点并不在 PHP 代码，而在于上传目录与 JSON 数据文件不可写。因此，部署后首先应检查目录权限是否允许 Web 服务用户写入。

```bash
chmod -R 755 /www/wwwroot/your-site/
chmod -R 777 /www/wwwroot/your-site/data/
chmod -R 777 /www/wwwroot/your-site/students/
chmod -R 777 /www/wwwroot/your-site/album/
chmod -R 777 /www/wwwroot/your-site/assets/images/overlays/
```

如果服务器安全策略要求更严格权限，应将这些目录的属主改为 Web 服务运行用户，并保证该用户具有写权限。对于 Nginx 环境，常见运行用户为 `www`；对于 Apache，则需根据实际运行用户调整。

---

## 四、PHP 建议配置

虽然系统会在接口初始化中调用 `ini_set` 尝试提高上传与执行限制，但生产环境依然建议在 `php.ini` 或站点级 PHP 配置中显式设置，避免上层限制覆盖应用设置。

| 配置项 | 建议值 | 说明 |
|---|---|---|
| `upload_max_filesize` | `100M` | 对齐当前后台初始化逻辑 |
| `post_max_size` | `105M` | 应略高于上传上限 |
| `max_execution_time` | `120` | 避免大文件上传或生成超时 |
| `memory_limit` | `256M` | 适配图片处理与全景资源 |
| `max_input_time` | `120` | 可选，建议同步提高 |

推荐示例：

```ini
upload_max_filesize = 100M
post_max_size = 105M
max_execution_time = 120
max_input_time = 120
memory_limit = 256M
```

如果是宝塔面板环境，可在对应 PHP 版本的“配置修改”中直接设置。修改后需重载 PHP-FPM 或 Web 服务。

---

## 五、Nginx 推荐配置

若使用 Nginx，除了 PHP 配置之外，还应确保站点配置允许较大的请求体，并合理控制 JSON 文件访问行为。前台页面会主动读取部分 JSON 文件，但并不应将敏感后台配置直接暴露为可下载资源。

推荐补充如下配置：

```nginx
client_max_body_size 105m;

location ~* \.json$ {
    add_header Cache-Control "no-cache";
}
```

如果需要对特定敏感文件做更严格限制，可按实际访问需求增加规则；但要注意，前台运行依赖部分 JSON 配置与数据文件，禁止过严会导致名册、前言、粒子等模块失效。

---

## 六、Apache 使用说明

如果服务器使用 Apache，项目自带的 `.htaccess` 与后台目录配置已经能覆盖大部分常见需求。部署后主要检查两件事：其一是 `AllowOverride` 已开启，其二是 PHP 上传限制没有低于项目要求。

对于 Apache 环境，不建议额外改动静态资源路径重写逻辑，以免影响后台生成页面后的相对路径引用。尤其在 10.5 版本中，普通模板与专属模板的脚本引用均已统一追加版本参数，若服务器缓存策略过于激进，应结合 `Cache-Control` 或文件版本查询参数一并管理。

---

## 七、首次上线检查清单

正式上线前，建议依次完成以下核查：

| 检查项 | 通过标准 |
|---|---|
| 后台登录页 | 能正常打开并进入后台 |
| 学生管理 | 能新建学生并生成 HTML 页面 |
| 信息编辑 | 能保存资料并重新生成页面 |
| 头像上传 | 上传后可在前台页面显示 |
| 贴纸上传 | 上传后后台与前台均可正常使用 |
| 粒子上传 | 自定义粒子可上传并出现在选择器中 |
| 班级相册 | 照片可上传，列表可读取 |
| 全景图 | 可上传并正常进入查看页 |
| 站点设置 | 粒子和字体设置可保存并影响前台 |

如果其中某一项失败，优先检查日志、权限、上传大小限制与 JSON 写入是否成功。

---

## 八、10.5 版本运行特性

10.5 版本在服务器运行层面需要特别关注三类改动。

第一类是 **粒子系统统一注册**。内置粒子已改为通过前台统一注册表维护，新增了 **枫叶、银杏、羽毛、心形、光泡、书页** 等效果，后台选择器与生成器映射也同步对齐。只要模板与公共脚本已部署到位，站点配置与学生配置中的粒子值即可直接生效。

第二类是 **模板运行时一致性增强**。普通学生页与专属学生页现在统一使用带版本参数的公共资源引用，并共享全局字体同步与粒子运行时逻辑。这意味着服务器缓存如果未刷新，可能出现“代码已更新、页面仍旧版本”的假象，因此升级后建议强制刷新浏览器缓存。

第三类是 **站点配置结构扩展**。当前 `data/site_config.json` 不仅管理页面粒子，还包含前言、特别致谢与字体配置。特别致谢条目支持 `avatar` 字段，全局字体配置位于 `typography.fontFamily` 与 `typography.fontSize`。运维层不建议直接手动改这些字段，除非明确知道当前数据结构。

---

## 九、常见故障与排查

### 1. 上传文件时报错或无响应

优先检查以下项目：目录是否可写、PHP 上传上限是否足够、Nginx `client_max_body_size` 是否过低、浏览器控制台是否存在 413 或 500 错误。若只有大文件失败而小文件正常，通常就是服务器上传限制导致。

### 2. 页面显示更新了，但样式或脚本没有变化

10.5 已通过版本参数降低缓存影响，但浏览器、CDN 或代理缓存仍可能保留旧文件。请先执行强制刷新；如果服务器前面挂了 CDN，也要同步清缓存。

### 3. 前台粒子没有显示

先确认页面粒子开关在后台未被关闭，再检查 `assets/js/common.js` 与 `assets/js/overlay.js` 是否已更新到同一版本，并确认页面模板引用的资源路径没有丢失版本参数。

### 4. 特别致谢头像不显示

当前配置结构要求头像路径写入 `site_config.json` 的 `acknowledgments[*].avatar`。如果数据存在但前台不显示，应检查文件是否真实存在，以及路径是否为站点可访问路径。

### 5. 修改字体大小后前台未变化

10.5 已补齐普通模板页与专属页的字体同步逻辑。若仍无变化，通常是旧页面引用了缓存脚本，或页面未重新加载到最新资源。

---

## 十、备份建议

项目不依赖数据库，因此备份策略相对简单，但应覆盖代码、数据与上传资源三部分。

| 备份对象 | 是否必须 | 原因 |
|---|---|---|
| `data/` | 必须 | 所有配置与学生信息都在这里 |
| `students/` | 必须 | 包含生成页面与所有学生资源 |
| `album/` | 必须 | 包含相册和全景数据 |
| `assets/images/overlays/` | 建议 | 自定义粒子素材在此目录 |
| `admin/`、`assets/` | 建议 | 保留当前代码版本，便于回滚 |

建议在每次大版本升级前做一次完整压缩备份，并单独备份 `site_config.json` 与 `students.json`，这样出现兼容问题时可以快速回滚。

---

## 十一、升级到 10.5 的建议步骤

如果服务器此前运行的是旧版本，推荐按以下顺序升级：先备份旧站点，再覆盖 `assets/`、`admin/`、模板文件与公共脚本，随后核查 `data/site_config.json` 的结构兼容性，最后刷新前后台页面并逐项验证粒子、字体、上传与生成链路。

最重要的是不要只覆盖单个脚本文件而遗漏模板、生成器或后台选择器，否则会出现“后台能选、前台不生效”或“前台有预设、后台列表缺失”的版本不一致问题。10.5 已将这部分统一为同一套粒子命名体系，部署时应保证整套文件同时更新。

---

## 十二、结语

同学录 10.5 依然保持“**无数据库、低依赖、可直接部署**”的设计方向，但随着后台能力扩展、模板数量增加、粒子系统增强，服务器配置的稳定性已经比早期版本更重要。只要权限、上传限制与缓存策略正确，当前版本可以稳定支持前台展示、后台维护、页面生成、贴纸与粒子上传、相册与全景管理等完整流程。

*同学录 v10.5 · 服务器配置说明*
