# 同学录专属页面开发指南

本文档旨在指导开发者和网站管理员如何使用“专属模板”系统，为特定的同学创建高度定制化的个人页面。

## 1. 什么是专属模板？

默认情况下，所有同学的页面都是由系统统一生成的（基于 `student.tpl.html`）。而**专属模板**允许你为特定的同学（标记为 `Owner`）上传完全自定义的 HTML 代码和配套资源（图片、音乐、CSS/JS 等）。

通过后台的“专属模板”管理界面，你可以模块化地管理这些资源，并一键部署到同学录中，同时保持访问量统计和名单数据的同步。

## 2. 如何创建专属同学？

1. 登录后台管理系统，点击右上角的 **+ 新建学生**。
2. 填写学生姓名和拼音代号（Slug，例如 `zhangsan`）。
3. **勾选“设为专属模板（Owner）”** 选项。
4. 点击“创建学生”。

创建完成后，该学生将出现在左侧菜单的 **🌟 专属模板** 列表中。

## 3. 目录结构与资源分类

每个专属同学在服务器上都有一个独立的资源文件夹，路径为：`exclusive/{slug}/`（例如 `exclusive/zhangsan/`）。

该目录下包含以下子目录，用于分类存放资源：

| 目录名称 | 用途说明 | 支持的文件格式 |
| :--- | :--- | :--- |
| `/` (根目录) | 存放核心页面代码，必须命名为 `index.html` | `.html`, `.htm` |
| `avatar/` | 存放头像图片 | `.jpg`, `.png`, `.webp` |
| `backgrounds/` | 存放页面背景图 | `.jpg`, `.png`, `.webp`, `.gif` |
| `music/` | 存放背景音乐文件 | `.mp3`, `.ogg`, `.m4a` |
| `stickers/` | 存放专属贴纸 | `.png`, `.gif`, `.webp` |
| `photos/` | 存放个人照片 | `.jpg`, `.png`, `.webp` |
| `assets/` | 存放自定义的 CSS、JS、字体等其他资源 | 任意格式 |

## 4. 资源引用规范（重要）

当你编写专属的 `index.html` 时，**必须使用相对路径**来引用你上传的资源。

因为最终部署时，你的 `index.html` 会被复制到 `students/{slug}.html`，而资源仍然保留在 `exclusive/{slug}/` 目录下。

### 相对路径示例

假设该同学的拼音代号（Slug）为 `zhangsan`：

*   **引用头像**：
    `<img src="../exclusive/zhangsan/avatar/zhangsan.jpg" alt="头像">`
*   **引用背景图**：
    `<div style="background-image: url('../exclusive/zhangsan/backgrounds/bg.jpg');"></div>`
*   **引用音乐**：
    `<audio src="../exclusive/zhangsan/music/bgm.mp3" autoplay loop></audio>`
*   **引用自定义 CSS/JS**：
    `<link rel="stylesheet" href="../exclusive/zhangsan/assets/style.css">`
    `<script src="../exclusive/zhangsan/assets/main.js"></script>`

**注意**：后台上传资源时，界面上会直接显示该资源的标准引用路径（如 `../exclusive/zhangsan/avatar/xxx.jpg`），你可以直接复制使用。

## 5. 上传与部署流程

1. **准备代码**：在本地编写好 `index.html`，并确保资源路径按照上述规范填写。
2. **上传资源**：进入后台 **🌟 专属模板**，选择对应的同学。分别在对应的分类下上传你的 HTML 文件、头像、音乐等。
    *   *提示：在上传贴纸、照片和自定义资源时，你可以输入“自定义文件名”以便于在代码中固定引用。*
3. **一键部署**：所有资源上传完毕后，点击页面底部的 **🚀 部署到同学录** 按钮。
4. **验证结果**：部署成功后，系统会将 `index.html` 复制到前台的 `students/` 目录下。点击“预览页面”即可查看最终效果。

## 6. 与系统功能的集成

虽然专属页面是自定义的，但它仍然与同学录的核心系统保持连通：

*   **访问统计**：专属页面会被计入系统的“已建档学生”总数。
*   **头像同步**：在专属模板中上传“头像”时，系统会自动将其同步到公共的 `students/avatars/` 目录，确保在首页花名册中能正确显示。
*   **音乐同步**：上传“音乐”时，系统会自动更新该同学的后台音乐配置，使其与公共播放器兼容。

---
*文档版本：1.0 | 更新日期：2026-03-29*
